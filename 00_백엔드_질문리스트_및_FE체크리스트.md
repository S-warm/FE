# 서버 시작 전 최소 필수 사항

---

## 📋 PART A: 백엔드 개발자에게 물어볼 질문 리스트

**이것을 복사해서 백엔드 개발자에게 보내세요.**

### 기본 정보

```
1. 현재 Spring 서버가 어디에 있나?
   [ ] 로컬 (localhost)
   [ ] EC2
   [ ] 기타: _____________

2. 서버 접속 주소는?
   http://________:______ 
   또는 
   https://________:______

3. Swagger/API 문서 URL은?
   http(s)://________:______/swagger-ui/index.html
   또는
   http(s)://________:______/api-docs

4. 현재 서버 상태는? (구동 중인가?)
   [ ] 구동 중
   [ ] 대기 중
   [ ] 아직 안 올림
```

### API 명세

```
5. POST /api/simulations 요청 바디의 정확한 DTO 필드명과 타입
   (Swagger에서 복사 붙여넣기)
   
   예:
   {
     "title": "string",
     "task": "string",
     "targetUrl": "string",
     "successCondition": {
       "type": "string (enum: CONTAINS, REGEX, EQUALS)",
       "value": "string"
     },
     ...
   }

6. POST /api/simulations 응답 형식은?
   [ ] 직접 { id, status, ... }
   [ ] 래핑됨 { data: {...}, result: true, ... }
   [ ] 기타: _____________

7. 응답의 정확한 예시 (실제 JSON 복사)
   {
     ...
   }

8. 인증이 필요한가?
   [ ] 예 → Authorization: Bearer ______ (테스트 토큰 주기)
   [ ] 아니오

9. CORS 설정은 되어 있나?
   localhost:5173 에서의 요청을 허용하나?
   [ ] 예
   [ ] 아니오
```

### 배포

```
10. EC2에 올렸다면, EC2의 Public IP 또는 Domain은?
    ________________________________________

11. HTTPS 포트는?
    [ ] 8443
    [ ] 443
    [ ] 기타: ______
```

---

## ✅ PART B: 백엔드 답변을 받은 후 FE 수정 체크리스트

**받은 정보를 이 순서대로 반영하세요.**

### 1단계: 기본 설정 (파일 수정)

```
☐ .env.development 파일 생성/수정
  VITE_API_BASE_URL=http(s)://[서버주소]:[포트]/api
  VITE_API_TIMEOUT=30000
  VITE_AUTH_TOKEN=[백엔드가 준 토큰] (필요한 경우만)
  VITE_MOCK_MODE=false

☐ http-client.ts 확인
  - baseURL이 import.meta.env.VITE_API_BASE_URL을 읽고 있나?
  - Bearer 토큰 자동 주입 코드 있나?
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }

☐ vite.config.ts 확인
  - CORS 프록시 설정 필요한가? (로컬에서만)
```

### 2단계: 타입 정의 (types/simulation.ts)

```
☐ src/types/simulation.ts 수정
  - 백엔드 DTO와 필드명 정확히 일치
  - enum 값들 정확히 일치
  
  예시:
  export interface CreateSimulationRequest {
    title: string;
    task: string;
    targetUrl: string;
    successCondition: {
      type: 'CONTAINS' | 'REGEX' | 'EQUALS';  ← 백엔드와 일치
      value: string;
    };
    // ... 나머지 필드들
  }
```

### 3단계: 응답 변환 (Adapter)

```
☐ adapters/simulation.adapter.ts 수정
  - 응답 형식이 { data: {...}, result: true } 라면:
    const backendDto = response.data.data;  // 래퍼 제거
  
  - 직접 응답이면:
    const backendDto = response.data;
  
  - 매핑 로직 확인:
    return {
      id: dto.id,
      status: this.mapStatus(dto.status),  // "RUNNING" → "running"
      // ... 다른 필드들
    };
```

### 4단계: Service 계층

```
☐ services/simulation.service.ts 수정
  - httpClient.post('/simulations', request) 호출
  - 응답에서 래퍼 제거 (필요하면)
  - Adapter.toDomain() 적용
  - 에러 처리 추가
```

### 5단계: 테스트

```
☐ Postman으로 테스트
  POST [서버주소]/api/simulations
  Body: 백엔드가 준 예시 JSON 복사
  → 200 OK 응답 확인

☐ npm run dev 실행
  $ npm run dev
  → http://localhost:5173 열기
  → 브라우저 개발자 도구 Network 탭 열기

☐ FE 화면에서 시뮬레이션 생성 버튼 클릭
  → /api/simulations 요청 보이나?
  → 200 응답 오나?
  → 에러 없나?

☐ 에러 있으면 확인
  [ ] Network 탭에서 요청 URL 올바른가?
  [ ] 응답 상태 코드? (401, 400, 500?)
  [ ] 에러 메시지?
  → 스크린샷 찍어서 백엔드에 보내기
```

---

## 🔍 트러블슈팅 (에러별)

```
에러: "Cannot GET /api/simulations"
→ .env의 VITE_API_BASE_URL 경로 확인
   http://localhost:8080/api ← 마지막에 /api 있나?

에러: "CORS error"
→ 백엔드의 WebMvcConfigurer 확인
   localhost:5173이 allowedOrigins에 있나?

에러: "401 Unauthorized"
→ .env에 VITE_AUTH_TOKEN 있나?
→ http-client.ts에서 Authorization 헤더 주입하나?

에러: "400 Bad Request"
→ 요청 필드명 백엔드 DTO와 일치하나?
→ 타입 일치하나? (number vs string?)

에러: "500 Internal Server Error"
→ 백엔드 로그 확인하라고 요청
```

---

## 최종 체크: 배포 전

```
☐ .env.production 파일 있나?
  VITE_API_BASE_URL=https://[EC2주소]:[포트]/api

☐ npm run build 성공하나?
  $ npm run build

☐ dist/ 폴더 생겼나?

☐ Mock 파일 코드에서 제거했나?
  grep -r "import.*simulations.json" src/
  (출력 없으면 OK)

☐ Vercel/S3 배포 설정 완료?
```

---

## 요약

### 백엔드에게 물어볼 것 (위 11개)

**가장 중요한 3개:**
1. 서버 주소 + 포트
2. POST /api/simulations 요청/응답 DTO 정확한 형식
3. 테스트 토큰 (인증 필요하면)

### FE 수정할 것 (위 체크리스트)

**가장 중요한 4개:**
1. .env.development 수정 (서버 주소)
2. types/simulation.ts 수정 (DTO 필드 일치)
3. Adapter 수정 (응답 래퍼 제거)
4. Postman + npm run dev 테스트

---

**이것만 하면 백엔드와 FE 연동 OK!**
