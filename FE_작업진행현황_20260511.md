# FE 작업 진행 현황 보고서
**작성일**: 2026-05-11  
**브랜치**: `jihyun` (최신 커밋: `2bad71f 스크린샷 빈 상태 처리 보완`)  
**평가**: **실 API 연결 직전 단계 (Step 21 ~ 22 미완료)**

---

## 1. 임무 완수 평가

### ✅ 완료된 것 (Step 1 ~ 20)
지현님이 다음 영역에서는 **구조 설계와 구현이 거의 완료**된 상태입니다:

| 영역 | 상태 | 세부 |
|------|------|------|
| **구조 설계** | ✅ 완료 | Mock → API 전환 아키텍처 확정 |
| **페이지 소비 구조** | ✅ 완료 | Mock 직접 참조 제거, Query/Service/Adapter 기반으로 전환 |
| **HTTP 기반 설계** | ✅ 완료 | HttpService Interface 정의, 도메인별 분리 |
| **인증 체계** | ✅ 설계 완료 | auth.store 구현, Zustand 기반 토큰 관리 |
| **Query Hook** | ✅ 완료 | React Query 통합, 12개 쿼리 훅 완성 |
| **Adapter 계층** | ✅ 완료 | 9개 adapter 구현 완료 |
| **Form 검증** | ✅ 완료 | 클라이언트 validation 규칙 정의 |
| **상태 패턴** | ✅ 완료 | Loading/Empty/Error 표준화 |

### ⚠️ 미완료된 것 (Step 21 ~ 22)

#### Step 21: 실 API raw 응답 기준 보정
- **상태**: 부분 완료 (약 30%)
- **블로커**: 백엔드 실 API 응답이 필요
- **남은 작업**:
  1. **WCAG 응답 구조** 확인 필요
     - simulation-level vs page-level 판정 필요
     - `wcag.adapter.ts`의 변환 로직 검증 대기
  2. **Heatmap 응답 구조** 확인 필요
     - `currentAgeGroup`, `pagination`, `errorPoints` 실제 구조 확인 필요
     - 좌표 정규화 (0~1, 0~100) 검증 대기

#### Step 22: 정책 확정
- **상태**: 미완료 (0%)
- **블로커**: 팀 협의 필요
- **남은 정책 항목**:
  1. **Persona Device** - 6종 → BE 3종 매핑 정책
  2. **endUrl** - FE-only 유지 여부
  3. **Status Enum** - 전역 정책 (BE response enum vs FE enum 동기화)
  4. **PDF/공유** - 다운로드 및 공유 버튼 정책
  5. **Refresh Token** - 자동 재발급 정책

---

## 2. 현재 코드 현황

### 파일 구성
```
src/
├── services/          (19 파일) ✅ 완료
│   ├── core/         (HTTP 클라이언트 기반 설계)
│   ├── simulation/   (Mock + Interface 완성)
│   └── result/       (6개 도메인 Mock + Interface)
├── adapters/         (12 파일) ✅ 완료
│   ├── simulation/
│   └── result/       (5개 탭용 adapter)
├── queries/          (12 파일) ✅ 완료
│   ├── simulation/   (3개 쿼리)
│   └── result/       (5개 쿼리)
├── types/            (24 파일) ✅ 완료
│   ├── api/          (API DTO)
│   ├── view-model/   (FE 뷰 모델)
│   └── ui.ts         (UI 타입)
├── pages/            (14 파일) ✅ 완료
│   ├── Simulation*   (2개)
│   └── result/       (7개)
├── store/            (4 파일) ✅ 완료
│   └── auth.store    (Zustand 기반)
├── validation/       (1 파일) ✅ 완료
└── components/       (구조 정리 완료)

총 182 TS/TSX 파일
```

### 핵심 레이어 통계
| 레이어 | 개수 | 상태 |
|--------|------|------|
| Mock Service | 6 | ✅ 완료 |
| HTTP Service Interface | 6 | ✅ 인터페이스만 (구현 부분 미완료) |
| Query Hooks | 12 | ✅ 완료 |
| Adapters | 9 | ✅ 완료 |
| Type Definitions | 24 | ✅ 완료 |

---

## 3. HTTP 서비스 구현 현황

### 구현 상태

```
✅ Mock 서비스 (완료)
├── simulationMockService
├── resultOverviewMockService
├── resultIssuesMockService
├── resultAiFixMockService
├── resultHeatmapMockService
└── resultWcagMockService

⚠️ HTTP 서비스 (Interface만 정의, 실구현 미완료)
├── simulationHttpService       → 3개 메서드 NotImplemented
├── resultOverviewHttpService   → 1개 메서드 NotImplemented
├── resultIssuesHttpService     → 1개 메서드 NotImplemented
├── resultAiFixHttpService      → 1개 메서드 NotImplemented
├── resultHeatmapHttpService    → 1개 메서드 NotImplemented
└── resultWcagHttpService       → 1개 메서드 NotImplemented
```

### 미구현 메서드 (총 8개)
1. `simulationHttpService.createSimulation()`
2. `simulationHttpService.getSimulationList()`
3. `simulationHttpService.getSimulationHeader()`
4. `resultOverviewHttpService.getOverview()`
5. `resultIssuesHttpService.getIssues()`
6. `resultAiFixHttpService.getAiFix()`
7. `resultHeatmapHttpService.getHeatmap()`
8. `resultWcagHttpService.getWcag()`

### 현재 아키텍처
```
Service Factory Pattern (src/services/core/service-factory.ts)
│
├─ useMockServices = true  → Mock 서비스 사용 (현재 상태)
└─ useMockServices = false → HTTP 서비스 사용 (NotImplemented 에러 발생)
```

---

## 4. 인증 체계 현황

### 현재 상태
```typescript
// src/store/auth.store.ts (Zustand)
- isAuthenticated: boolean
- user: { initials, username } | null
- canLogin(username, password): boolean
- login(username): void
- logout(): void

// Mock 자격증명
- username: "admin"
- password: "123"
```

### 남은 작업
- ✅ Local Storage persistence (이미 구현됨)
- ⚠️ HTTP 기반 로그인/회원가입 구현 필요
- ⚠️ Refresh Token 자동 재발급 정책 미정

---

## 5. 최신 커밋 히스토리

```
2bad71f (현재) - 스크린샷 빈 상태 처리 보완
db0e1ac - 백엔드 직전 최종 정리
de8fb2c - 결과 탭 2차 쿼리 구조로 전환
cbf6a81 - 결과 탭 1차 쿼리 구조로 전환
ee49a8f - 시뮬레이션 생성 흐름 실연결
674e893 - 공통 진입점 쿼리 구조로 전환
db4ec69 - 점진적 실장 현황 문서 추가
4eeb653 - 공통 상태 컴포넌트 추가
068587b - 리액트 쿼리 골격 추가
```

---

## 6. 실 API 연결 직전 확인 사항

### Step 21을 마무리하기 위해 필요한 것

**1️⃣ WCAG API 응답 샘플**
```bash
GET /api/simulations/{id}/wcag

현재 예상 구조:
{
  "results": [
    {
      "page": "string",
      "severity": "error|warning|notice",
      "count": number,
      "issues": [...]
    }
  ]
}

확인 필요:
- simulation-level? page-level?
- severity enum 값
- results 배열 구조
```

**2️⃣ Heatmap API 응답 샘플**
```bash
GET /api/simulations/{id}/heatmap

현재 예상 구조:
{
  "currentAgeGroup": "string",
  "points": [
    {
      "x": 0-1,      // 정규화 필요?
      "y": 0-1,      // 정규화 필요?
      "count": number
    }
  ],
  "pagination": {...},
  "errorPoints": [...]
}

확인 필요:
- x, y 좌표 범위 (0~1? 0~100? pixel?)
- currentAgeGroup 형식
- pagination 구조
- errorPoints 정의
```

**3️⃣ Adapter 검증 필요**
- `src/adapters/result/wcag.adapter.ts`
- `src/adapters/result/heatmap.adapter.ts`

### Step 22를 위해 필요한 것

**회의 안건 정리** (다음 캡스톤 회의):
1. Persona device 정책 (6종 → 3종)
2. endUrl FE-only 유지 여부
3. Status enum 전역 동기화
4. PDF/공유 버튼 구현 여부
5. Refresh token 정책

---

## 7. 다음 단계별 작업 계획

### 📌 우선순위 1: Step 21 완료 (수종 님과 협력)
```
Timeline: 실 BE API 가동 후 즉시
Action:
  1. raw JSON 응답 캡처
  2. wcag.adapter.ts 검증
  3. heatmap.adapter.ts 검증
  4. HTTP 서비스 구현 (8개 메서드)

Owner: 지현님 (BE 응답 수령 후)
```

### 📌 우선순위 2: HTTP 서비스 구현
```
Timeline: Step 21 WCAG/Heatmap 구조 확정 후
Action:
  1. simulationHttpService 구현
  2. resultOverviewHttpService 구현
  3. resultIssuesHttpService 구현
  4. resultAiFixHttpService 구현
  5. resultHeatmapHttpService 구현
  6. resultWcagHttpService 구현

Template:
export const {name}HttpService: {Name}Service = {
  async get{method}(params) {
    const response = await httpClient.get(`/api/path`, { params })
    return {name}Adapter(response.data)
  }
}
```

### 📌 우선순위 3: 인증 / 정책 확정
```
Timeline: Step 22 회의 후
Action:
  1. HTTP 로그인 구현
  2. HTTP 회원가입 구현
  3. Refresh token 정책 구현
  4. Status enum 전역 정책 반영
  5. PDF/공유 정책 반영
```

---

## 8. 지현님의 역할 정리

### 이미 완료한 것 (👏)
- ✅ 아키텍처 설계 (mock → api)
- ✅ 타입 체계 정의 (api DTO vs view-model)
- ✅ 페이지 구조 전환 (query/service/adapter 기반)
- ✅ Mock 기반 동작 완성
- ✅ 에러 처리 패턴 정의
- ✅ Form 검증 로직

### 지금부터 할 것
1. **데이터 정합성 검증** (BE raw 응답 vs FE adapter)
2. **HTTP 서비스 구현** (8개 메서드)
3. **정책 협의** (팀 회의)
4. **통합 테스트** (실제 BE와 연결)

### 더는 할 게 없는 것
- ❌ 구조 재설계 (이미 충분히 안정적)
- ❌ 타입 대규모 변경 (이미 완성됨)
- ❌ 페이지 리팩토링 (현 구조가 최적)

---

## 9. 최종 평가

| 평가 항목 | 점수 | 코멘트 |
|----------|------|--------|
| 아키텍처 안정성 | ⭐⭐⭐⭐⭐ | 매우 우수. 확장 가능 |
| 코드 정리도 | ⭐⭐⭐⭐⭐ | Mock 참조 완전 제거 |
| 테스트 가능성 | ⭐⭐⭐⭐⭐ | Service/Adapter 분리 완벽 |
| 실 API 준비도 | ⭐⭐⭐⭐☆ | Interface 정의됨, 구현만 남음 |
| 전체 완성도 | ⭐⭐⭐⭐☆ | 80% (Step 21~22 미완료) |

### 종합 판정
**"프론트엔드는 현재 백엔드 통합 직전 단계에 와 있습니다."**

- `VITE_USE_MOCK_SERVICES=false`로 바꾸면 지금 당장 HTTP 서비스로 전환 가능 (NotImplemented 에러 발생하지만 구조적 문제는 없음)
- 실 API raw 응답을 받는 순간 adapter 수정 → HTTP 서비스 구현으로 바로 진행 가능
- 정책 협의는 병렬 진행 가능

---

## 10. 지현님께 제안하는 즉시 액션

### 🎯 오늘 중
```bash
# 1. 현재 상태 검증
cd Frontend
npm run build    # 빌드 성공? 
npm run dev      # 로컬 dev 실행 성공?

# 2. Mock 동작 확인
# VITE_USE_MOCK_SERVICES=true 상태에서
# - 로그인 → 시뮬레이션 생성 → 결과 페이지까지 전체 흐름 테스트
```

### 🎯 수종 님과
```
"BE 실 API 한 번만 띄워서 다음 엔드포인트의 raw JSON을 캡처해 줄 수 있을까요?
- POST /api/simulations (생성 응답)
- GET /api/simulations (목록)
- GET /api/simulations/{id} (헤더)
- GET /api/simulations/{id}/wcag
- GET /api/simulations/{id}/heatmap
- GET /api/simulations/{id}/issues
- GET /api/simulations/{id}/overview
- GET /api/simulations/{id}/ai-fix

이거 확보되면 Step 21 마감 가능합니다."
```

### 🎯 다음 회의에서
```
Step 22 정책 5가지 안건 올리기:
1. Persona device 정책
2. endUrl 정책
3. Status enum 정책
4. PDF/공유 정책
5. Refresh token 정책
```

---

## 📎 참고: 바로 확인할 파일들

**구조 이해용**:
- `src/services/core/service-factory.ts` - Mock/HTTP 선택 로직
- `src/adapters/result/result-overview.adapter.ts` - Adapter 패턴 예시
- `src/queries/simulation/use-simulation-list-query.ts` - Query Hook 예시

**HTTP 구현용**:
- `src/services/simulation/simulation.mock.service.ts` - 구현해야 할 인터페이스
- `src/types/api/simulation/` - DTO 타입들

**타입 정의용**:
- `src/types/view-model/result/result-overview.ts`
- `src/types/api/simulation/simulation-overview.response.ts`

---

**작성**: Claude (AI Assistant)  
**검토 대상**: 지현님, 수종님  
**다음 리뷰**: BE API 구동 후 재작성 예정
