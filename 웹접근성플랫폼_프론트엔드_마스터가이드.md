# 웹 접근성 시뮬레이션 플랫폼
## 프론트엔드 마스터 배포 가이드

**v1.0 | 2026-05-18**

---

## Executive Summary

이 가이드는 프론트엔드 개발자가 로컬 목업 데이터 환경에서 AWS EC2에 배포된 실제 Spring 백엔드 API로 안전하게 전환하고, 최종 프로덕션 배포까지 완료하기 위한 시니어 아키텍트급 참고서입니다.

**현재 프로젝트 상태**: 프론트엔드 92% 완료, 백엔드/AI는 EC2 배포 완료. 이제 두 시스템의 안전한 통합 연동과 배포 파이프라인 구축이 남은 단계입니다.

---

# Chapter 1: 임시 JSON 관리 전략 (매우 중요)

## Q) 백엔드 팀이 준 임시 JSON으로 프론트엔드를 개발하고, 나중에 Spring 서버에서 API로 받는데 이게 맞는 흐름인가?

### ✅ YES. 이것은 100% 정상적이고 업계 표준 개발 패턴입니다.

모든 성숙한 프로젝트는 다음과 같은 3-Phase 흐름을 따릅니다:

| Phase | 개발 단계 | 주요 목표 |
|-------|---------|--------|
| **Phase 1** | 로컬 Mock JSON | UI/로직 완성, API 계약 정의 |
| **Phase 1.5-2.0** | 백엔드 임시 JSON | 프론트엔드 통합 테스트 (현재) |
| **Phase 3** | 실제 Spring API | 배포 및 프로덕션 안정화 |

당신의 프로젝트는 현재 Phase 1.5-2.0 구간에 있습니다:
- **Phase 1 초기**: 로컬 Mock JSON으로 UI/로직/컴포넌트 완성
- **Phase 1 말기**: 백엔드 팀이 임시 JSON 응답 제공 → **현재 상황**
- **Phase 2 예정**: EC2 Spring API 응답 수신 → 배포

---

## Q) 임시 JSON은 언제 지워?

### 답: 물리적으로 "삭제"하는 게 아니라, 프론트엔드 설정에서 API 엔드포인트를 바꾸는 것입니다.

| 단계 | 타이밍 | 액션 아이템 |
|------|--------|----------|
| **지금** | 현재 | 백엔드 팀의 실제 API 응답 확인 (Swagger) |
| **Phase 2** | 1주 후 | .env 파일에서 API_BASE_URL을 EC2로 변경 (파일 제거 X) |
| **Phase 3** | 배포 1주 전 | 코드에서 Mock 파일 import 제거, git에서 삭제 |
| **배포 후** | 배포 후 | 물리적 파일 삭제는 선택 (이미 로드 안 됨) |

---

## 핵심 원칙: 환경 변수(Environment Variables)로 제어

당신의 프론트엔드 architecture에서 API 호출은 이런 구조여야 합니다:

### service-config.ts

```typescript
// service-config.ts
export const SERVICE_CONFIG = {
  API_BASE_URL: import.meta.env.VITE_API_BASE_URL || 'http://localhost:3000/api',
  TIMEOUT: 30000,
  RETRY_COUNT: 3,
};

// 혹은 더 명시적으로
export const SERVICE_CONFIG = {
  // LOCAL DEVELOPMENT
  MOCK_MODE: import.meta.env.VITE_MOCK_MODE === 'true',

  // PRODUCTION
  API_BASE_URL: import.meta.env.VITE_API_BASE_URL,
  // EC2 예시: https://ec2-52-xxx-xxx-xxx.compute-1.amazonaws.com:8443/api
  // 또는: https://your-backend-domain.com/api
};
```

### .env 파일들

```bash
# .env.development (로컬 개발)
VITE_API_BASE_URL=http://localhost:8080/api
VITE_MOCK_MODE=false

# .env.production (배포 전 테스트)
VITE_API_BASE_URL=https://ec2-52-xxx-xxx-xxx.compute-1.amazonaws.com:8443/api
VITE_MOCK_MODE=false

# .env.staging (스테이징 테스트)
VITE_API_BASE_URL=https://staging.your-domain.com/api
VITE_MOCK_MODE=false
```

---

## 임시 JSON 파일의 실제 제거 시점

### 1. Phase 2.5 (오늘~1주일 후): 백엔드 팀의 실제 API 응답 확인

- Swagger 또는 Postman으로 백엔드 API 검증
- 프론트엔드 .env 파일에서 API_BASE_URL을 EC2 주소로 변경 (임시 JSON 파일 자체는 유지)

### 2. Phase 3 (배포 전 1주일): 실제 API 응답이 완전히 안정화된 후

- 프론트엔드 코드에서 Mock Data Import 구문 제거
- git에서 Mock JSON 파일 삭제 (`git rm --cached`)
- `.gitignore`에 Mock 폴더 추가 후 커밋

### 3. Production Deployment: 배포 후

- `VITE_MOCK_MODE=false` 만 확인하고 배포
- Mock JSON 파일의 물리적 존재는 상관없음 (로드되지 않음)

---

# Chapter 2: EC2 원격 서버 실연결 (필수 정보 수집)

Spring 백엔드 팀에게 반드시 확보해야 할 정보:

| 정보 항목 | 예시 | 참고 |
|----------|-----|------|
| **EC2 Public IP** | 52.xxx.xxx.xxx | AWS Console > EC2 > Instances |
| **Domain Name** | api.example.com | Route 53 또는 외부 DNS |
| **HTTP Port** | 8080 | Spring Boot 기본값 |
| **HTTPS Port** | 8443 | SSL/TLS 바인딩 |
| **Swagger URL** | /swagger-ui/index.html | Spring Doc 기본값 |
| **Auth Header** | Authorization: Bearer {JWT} | 필요 시 백엔드 확인 |

---

## Step 1: Swagger/OpenAPI 문서 확인

브라우저에서 이동:

```
https://{EC2_PUBLIC_IP_OR_DOMAIN}:8443/swagger-ui/index.html

또는 OpenAPI JSON:
https://{EC2_PUBLIC_IP_OR_DOMAIN}:8443/api-docs
```

이곳에서 다음을 확인:
- ✓ POST /api/simulations (시뮬레이션 생성)
- ✓ GET /api/simulations (목록)
- ✓ GET /api/simulations/{id} (상태 조회)
- ✓ 요청/응답 DTO 스키마
- ✓ HTTP 상태 코드 정의

---

## Step 2: Postman으로 API 핸드셰이크 테스트

Postman 또는 cURL로 기본 연동 테스트:

```bash
# Postman으로 이 요청 테스트
POST https://ec2-52-xxx-xxx-xxx.compute-1.amazonaws.com:8443/api/simulations

Headers:
Content-Type: application/json
Authorization: Bearer {JWT_TOKEN_IF_REQUIRED}

Body (JSON):
{
  "title": "Test Simulation",
  "task": "Find contact information",
  "targetUrl": "https://example.com",
  "successCondition": {
    "type": "contains",
    "value": "phone"
  },
  "userProfile": {
    "digitalLiteracy": "INTERMEDIATE",
    "personaDevice": "DESKTOP",
    "ageGroup": "20s",
    "visionImpairment": "NONE",
    "attentionLevel": "NORMAL"
  },
  "demographicDistribution": {
    "teens": 0,
    "twenties": 10,
    "thirties": 5,
    "forties": 3,
    "fifties": 2,
    "sixties": 0
  }
}

예상 응답 (200 OK):
{
  "id": "sim_12345abcde",
  "status": "RUNNING",
  "createdAt": "2026-05-18T10:30:00Z",
  "progress": 0
}
```

---

## Step 3: .env 파일에 EC2 주소 바인딩

프로젝트 루트의 `.env.development` 수정:

```bash
# .env.development
VITE_API_BASE_URL=https://ec2-52-xxx-xxx-xxx.compute-1.amazonaws.com:8443/api
VITE_API_TIMEOUT=30000
VITE_MOCK_MODE=false

# 필요 시 JWT 토큰 (로컬 테스트용)
VITE_AUTH_TOKEN=your_jwt_token_here_if_required
```

---

## Step 4: http-client.ts 구성 (Bearer 토큰 자동 주입)

http-client.ts에서 EC2 주소 대응:

```typescript
// http-client.ts
import axios, { AxiosInstance, AxiosError } from 'axios';

export class ApiServiceError extends Error {
  constructor(
    public statusCode: number,
    public details?: Record<string, any>
  ) {
    super(`API Error: ${statusCode}`);
    this.name = 'ApiServiceError';
  }
}

export const createApiClient = (): AxiosInstance => {
  const client = axios.create({
    baseURL: import.meta.env.VITE_API_BASE_URL,
    timeout: import.meta.env.VITE_API_TIMEOUT || 30000,
    headers: {
      'Content-Type': 'application/json',
    },
  });

  // 요청 전 Bearer 토큰 자동 주입
  client.interceptors.request.use((config) => {
    const token = import.meta.env.VITE_AUTH_TOKEN ||
                  localStorage.getItem('auth_token');
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  });

  // 응답 에러 처리 (중앙화)
  client.interceptors.response.use(
    (response) => response,
    (error: AxiosError) => {
      if (error.response?.status === 401) {
        // 토큰 만료 처리
        localStorage.removeItem('auth_token');
        window.location.href = '/login';
      }
      throw new ApiServiceError(
        error.response?.status || 0,
        error.response?.data
      );
    }
  );

  return client;
};

export const httpClient = createApiClient();
```

---

# Chapter 3: 데이터 송수신 정합성 검증

프론트엔드와 백엔드 간의 JSON 스키마 불일치는 런타임 에러의 주요 원인입니다.

---

## 프로세스 1: Swagger DTO 크로스 체크

1. 백엔드 Swagger에서 CreateSimulationRequest DTO 확인
2. 프론트엔드 `types/simulation.ts`와 필드 매핑
3. 형식 불일치(boolean vs string, number vs string 등) 사전 정정

### Swagger 정의 예시 (Spring DTO)

```java
CreateSimulationRequest {
  title: string
  task: string
  targetUrl: string
  successCondition: {
    type: string (enum: CONTAINS, REGEX, EQUALS)
    value: string
  }
  userProfile: {
    digitalLiteracy: string (enum: BASIC, INTERMEDIATE, ADVANCED)
    personaDevice: string (enum: DESKTOP, MOBILE, TABLET)
    ageGroup: string (enum: teens, twenties, thirties, ...)
    visionImpairment: string (enum: NONE, MILD, MODERATE, SEVERE)
    attentionLevel: string (enum: LOW, NORMAL, HIGH)
  }
  demographicDistribution: {
    teens: number (0-100)
    twenties: number (0-100)
    thirties: number (0-100)
    forties: number (0-100)
    fifties: number (0-100)
    sixties: number (0-100)
  }
}
```

### 프론트엔드 타입 정의 (TypeScript)

```typescript
export interface CreateSimulationRequest {
  title: string;
  task: string;
  targetUrl: string;
  successCondition: {
    type: 'CONTAINS' | 'REGEX' | 'EQUALS';
    value: string;
  };
  userProfile: {
    digitalLiteracy: 'BASIC' | 'INTERMEDIATE' | 'ADVANCED';
    personaDevice: 'DESKTOP' | 'MOBILE' | 'TABLET';
    ageGroup: 'teens' | 'twenties' | 'thirties' | 'forties' | 'fifties' | 'sixties';
    visionImpairment: 'NONE' | 'MILD' | 'MODERATE' | 'SEVERE';
    attentionLevel: 'LOW' | 'NORMAL' | 'HIGH';
  };
  demographicDistribution: {
    teens: number;
    twenties: number;
    thirties: number;
    forties: number;
    fifties: number;
    sixties: number;
  };
}
```

---

## 프로세스 2: Adapter 레이어 응답 매핑

백엔드 응답 래퍼 제거 후 UI 모델로 변환:

```typescript
// services/simulation.service.ts
export class SimulationService {
  async createSimulation(
    request: CreateSimulationRequest
  ): Promise<SimulationResult> {
    const response = await httpClient.post<ApiResponse<SimulationDTO>>(
      '/simulations',
      request
    );

    // 백엔드 응답 구조 (Swagger 확인)
    // {
    //   "data": { ... simulation data ... },
    //   "result": true,
    //   "payload": { ... }
    // }

    // 래퍼 제거 + Adapter로 UI 모델로 변환
    return SimulationAdapter.toDomain(response.data.data);
  }
}

// adapters/simulation.adapter.ts
export class SimulationAdapter {
  static toDomain(dto: SimulationDTO): SimulationResult {
    return {
      id: dto.id,
      title: dto.title,
      status: this.mapStatus(dto.status), // RUNNING → 'pending' 같이 UI 상태로 변환
      progress: dto.progressPercentage || 0,
      createdAt: new Date(dto.createdAt),
      estimatedCompletionTime: dto.estimatedCompletionTime,
      results: dto.results ? this.mapResults(dto.results) : null,
    };
  }

  private static mapStatus(
    status: string
  ): 'pending' | 'running' | 'completed' | 'failed' {
    const statusMap = {
      'PENDING': 'pending',
      'RUNNING': 'running',
      'COMPLETED': 'completed',
      'FAILED': 'failed',
    };
    return (statusMap[status as keyof typeof statusMap] || 'pending') as any;
  }

  private static mapResults(
    results: SimulationResultsDTO
  ): SimulationResults {
    return {
      accessibilityIssues: results.issues?.map(issue => ({
        id: issue.id,
        level: issue.severity, // CRITICAL → 'critical'
        message: issue.description,
        wcagCriteria: issue.wcagRef,
        suggestedFix: issue.recommendation,
      })) || [],
      wcagComplianceScore: results.wcagScore || 0,
      heatmapUrl: results.heatmapDataUrl,
      aiSuggestions: results.aiRecommendations || [],
    };
  }
}
```

---

## 프로세스 3: React Query Hook 통합

useQuery/useMutation으로 실제 API와 연동:

```typescript
// hooks/useSimulation.ts
import { useMutation, useQuery } from '@tanstack/react-query';
import { SimulationService } from '../services/simulation.service';
import { CreateSimulationRequest } from '../types/simulation';

export const useCreateSimulation = () => {
  return useMutation({
    mutationFn: (request: CreateSimulationRequest) =>
      SimulationService.createSimulation(request),
    onError: (error: ApiServiceError) => {
      console.error('Failed to create simulation:', error.statusCode, error.details);
      // UI에 에러 메시지 표시
    },
    retry: 3, // 자동 재시도
  });
};

export const useSimulationStatus = (simulationId: string | null) => {
  return useQuery({
    queryKey: ['simulation', simulationId],
    queryFn: () => SimulationService.getSimulation(simulationId!),
    enabled: !!simulationId, // simulationId가 있을 때만 실행
    refetchInterval: 2000, // 2초마다 상태 조회 (폴링)
    retry: 5,
  });
};

// 컴포넌트에서 사용
const CreateSimulationForm = () => {
  const { mutate, isLoading, error } = useCreateSimulation();

  const handleSubmit = (data: CreateSimulationRequest) => {
    mutate(data);
  };

  if (error instanceof ApiServiceError) {
    return <ErrorAlert code={error.statusCode} details={error.details} />;
  }

  return <form onSubmit={handleSubmit}>{/* ... */}</form>;
};
```

---

# Chapter 4: 프론트엔드 배포 전략

## 배포 옵션 비교: Vercel vs AWS S3 + CloudFront

| 항목 | Vercel | AWS S3 + CloudFront |
|------|--------|-----------------|
| **셋업** | 매우 쉬움 (git push) | 중간~복잡 |
| **CI/CD** | 내장됨 | CodePipeline 필요 |
| **SPA 라우팅** | 자동 (vercel.json) | CloudFront Error Pages |
| **API 프록시** | Edge Functions | CloudFront Behavior |
| **비용** | 무료~저비용 | 저~중비용 |
| **권장** | 스타트업, MVP | 엔터프라이즈 |

---

## 옵션 A: Vercel (권장 - 소규모 스타트업)

### 장점:

- Edge Functions로 API 라우팅/프록시 가능 (CORS 우회)
- git push만으로 자동 배포 (CI/CD 내장)
- SPA 라우팅 자동 지원 (vercel.json 간단)
- 무료 SSL/TLS (자동 HTTPS)

### vercel.json

```json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "env": {
    "VITE_API_BASE_URL": {
      "production": "https://ec2-52-xxx-xxx-xxx.compute-1.amazonaws.com:8443/api"
    }
  },
  "routes": [
    { "src": "^/static/(.*)", "dest": "/static/$1" },
    { "src": "^/.*", "dest": "/index.html" }
  ]
}
```

---

## 옵션 B: AWS S3 + CloudFront (권장 - 엔터프라이즈)

### 장점:

- 기존 AWS 인프라(EC2)와 통합
- CloudFront Distribution으로 API 요청 프록시 가능
- OAI (Origin Access Identity)로 S3 직접 접근 방지
- IAM 정책으로 접근 제어

### CloudFront Distribution 설정 (Terraform)

```hcl
resource "aws_cloudfront_distribution" "frontend" {
  origin {
    domain_name = aws_s3_bucket.frontend.bucket_regional_domain_name
    origin_id   = "S3-frontend"

    s3_origin_config {
      origin_access_identity = aws_cloudfront_origin_access_identity.oai.cloudfront_access_identity_path
    }
  }

  # API 백엔드를 2번째 origin으로 추가
  origin {
    domain_name = "ec2-52-xxx-xxx-xxx.compute-1.amazonaws.com"
    origin_id   = "backend-api"

    custom_origin_config {
      http_port              = 8080
      https_port             = 8443
      origin_protocol_policy = "https-only"
      origin_ssl_protocols   = ["TLSv1.2"]
    }
  }

  default_cache_behavior {
    allowed_methods  = ["GET", "HEAD", "OPTIONS"]
    cached_methods   = ["GET", "HEAD"]
    target_origin_id = "S3-frontend"

    forwarded_values {
      query_string = false
      cookies {
        forward = "none"
      }
    }

    viewer_protocol_policy = "redirect-to-https"
    min_ttl                = 0
    default_ttl            = 3600
    max_ttl                = 86400
  }

  # /api/* 요청은 백엔드로 라우팅
  ordered_cache_behavior {
    path_pattern     = "/api/*"
    allowed_methods  = ["GET", "POST", "PUT", "DELETE", "OPTIONS", "PATCH", "HEAD"]
    cached_methods   = ["GET", "HEAD"]
    target_origin_id = "backend-api"

    forwarded_values {
      query_string = true
      headers {
        header_names = ["Authorization", "Content-Type", "Accept"]
      }
      cookies {
        forward = "all"
      }
    }

    viewer_protocol_policy = "https-only"
    min_ttl                = 0
    default_ttl            = 0
    max_ttl                = 0
  }

  restrictions {
    geo_restriction {
      restriction_type = "none"
    }
  }

  viewer_certificate {
    cloudfront_default_certificate = true
    # 또는 ACM 인증서 사용
    # acm_certificate_arn      = aws_acm_certificate.frontend.arn
    # ssl_support_method       = "sni-only"
    # minimum_protocol_version = "TLSv1.2_2021"
  }

  enabled = true
  is_ipv6_enabled = true
}
```

---

# Chapter 5: 런타임 트러블슈팅

## 문제 1: CORS (Cross-Origin Resource Sharing) 에러

### 증상:
브라우저 콘솔에서 "Access to XMLHttpRequest at '...ec2....' from origin 'localhost:5173' has been blocked by CORS policy"

### 원인:
프론트엔드(localhost)와 백엔드(EC2)가 다른 도메인/포트이므로 브라우저의 같은 출처 정책(Same-Origin Policy) 위반

### 백엔드 (Spring Boot)에서 해결

```java
// WebMvcConfigurer 구현
import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.CorsRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

@Configuration
public class CorsConfig implements WebMvcConfigurer {
  @Override
  public void addCorsMappings(CorsRegistry registry) {
    registry.addMapping("/api/**")
      .allowedOrigins(
        "http://localhost:5173",      // 로컬 개발
        "http://localhost:3000",      // 또는 다른 포트
        "https://your-frontend-domain.com"  // 프로덕션
      )
      .allowedMethods("GET", "POST", "PUT", "DELETE", "OPTIONS", "PATCH")
      .allowedHeaders("*")
      .allowCredentials(true)
      .maxAge(3600);
  }
}
```

### 로컬 테스트 시 CORS 우회 (임시)

개발 단계에서만 사용 (프로덕션에서는 금지):

```typescript
// vite.config.ts
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    proxy: {
      '/api': {
        target: 'https://ec2-52-xxx-xxx-xxx.compute-1.amazonaws.com:8443',
        changeOrigin: true,
        secure: false, // 자체 서명 인증서 무시 (개발 전용)
        rewrite: (path) => path.replace(/^\\/api/, '/api'),
      },
    },
  },
});
```

이 설정으로 로컬에서 /api 요청은 자동으로 EC2 주소로 프록시됨

---

## 문제 2: Mixed Content (보안 경고)

### 증상:
HTTPS 배포 사이트에서 HTTP EC2 API 호출 시 브라우저가 차단

### 원인:
보안상 HTTPS 페이지는 HTTP 리소스 로드 불가

### 해결책 A: 백엔드에 SSL/TLS 적용 (권장)

AWS ACM (Certificate Manager)으로 자동 인증서 관리:

```bash
# 1. EC2 보안 그룹 규칙 추가 (HTTPS 포트 개방)
inbound rule: HTTPS (443) from 0.0.0.0/0

# 2. EC2 내부에 Certbot 설치 (자동 갱신)
sudo apt-get install certbot python3-certbot-nginx
sudo certbot certonly --standalone -d your-backend-domain.com

# 3. 또는 AWS ACM + ALB (Application Load Balancer) 사용
#   - ALB에 ACM 인증서 연결
#   - ALB → EC2 인스턴스 라우팅

# 4. 프론트엔드 .env 수정
VITE_API_BASE_URL=https://your-backend-domain.com/api
```

### 해결책 B: CloudFront로 프록시 (AWS 환경)

CloudFront Distribution이 /api/* 요청을 백엔드로 라우팅하면, 브라우저에서는 cloudfront.net 도메인만 보임 (섞인 콘텐츠 문제 없음)

→ Chapter 4 '옵션 B'의 CloudFront 설정 참고

---

## 문제 3: 인증서 검증 실패 (self-signed certificate)

### 증상:
"certificate_verify_failed" 또는 ERR_INSECURE_RESPONSE

### 원인:
EC2가 자체 서명 인증서 또는 만료된 인증서 사용

### 로컬 개발 환경에서만 임시 우회

```typescript
// http-client.ts
import axios from 'axios';
import https from 'https';

const httpsAgent = new https.Agent({
  rejectUnauthorized: process.env.NODE_ENV === 'production', // 프로덕션에서는 true
});

export const httpClient = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  httpsAgent, // 자체 서명 인증서 무시 (개발 전용)
});

// 또는 더 명시적으로
if (import.meta.env.DEV) {
  // 개발 환경에서만 인증서 검증 비활성화
  process.env.NODE_TLS_REJECT_UNAUTHORIZED = '0';
}
```

**⚠️ 프로덕션에서는 반드시 정규 SSL/TLS 인증서(Let's Encrypt, AWS ACM) 사용**

---

# Appendix: 배포 체크리스트

| 카테고리 | 항목 | 상태 |
|---------|------|------|
| **백엔드** | Swagger 확인 | ☐ |
| **백엔드** | CORS 설정 (WebMvcConfigurer) | ☐ |
| **백엔드** | SSL/TLS 인증서 | ☐ |
| **프론트엔드** | .env.production 수정 | ☐ |
| **프론트엔드** | service-config.ts 확인 | ☐ |
| **프론트엔드** | http-client.ts Bearer Token | ☐ |
| **테스트** | Postman API 테스트 | ☐ |
| **테스트** | 로컬에서 EC2 연동 테스트 | ☐ |
| **테스트** | React Query Hook 통합 | ☐ |
| **배포** | npm run build (빌드 테스트) | ☐ |
| **배포** | Vercel 또는 S3+CF 배포 | ☐ |
| **배포** | 프로덕션 스모크 테스트 | ☐ |

---

# 결론

1. 현재 임시 JSON 방식은 정상이며, 환경 변수로 관리하면 됩니다.

2. EC2 주소는 `.env` 파일과 `service-config.ts`에서만 관리합니다.

3. 백엔드 팀의 Swagger와 크로스 체크하여 DTO 필드를 정확히 매핑합니다.

4. CORS와 Mixed Content는 백엔드의 WebMvcConfigurer와 SSL/TLS로 해결합니다.

5. 배포는 Vercel(간단) 또는 AWS S3+CloudFront(통합)를 선택합니다.

6. 배포 후 스모크 테스트(Smoke Test)로 전체 연동을 최종 검증합니다.

---

**문서 작성일**: 2026-05-18  
**버전**: 1.0  
**대상자**: React + Vite 프론트엔드 개발자 (시니어 수준)  
**문의**: skykong14@gmail.com
